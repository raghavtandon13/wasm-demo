{
  "installCommand": "curl https://sh.rustup.rs -sSf | sh -s -- -y && export PATH=\"$HOME/.cargo/bin:$PATH\" && rustup target add wasm32-unknown-unknown && cargo install trunk",
  "buildCommand": "export PATH=\"$HOME/.cargo/bin:$PATH\" && trunk build --release",
  "outputDirectory": "dist"
}
